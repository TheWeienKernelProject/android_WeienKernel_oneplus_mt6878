menuconfig WK_FUTURE
    bool "WK Future Support"
    default y
    help
      Enable WK Future feature support.

if WK_FUTURE

config WK_FUTURE_EXP_PROTECT_PARTITION
    bool "Protect some devices partition"
    default n
    help
      protect device partition.

endif
